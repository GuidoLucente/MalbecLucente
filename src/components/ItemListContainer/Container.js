import React from "react";

const ItemListContainer = ({ greeting }) => {
    return(
        <div className="Style">
            <span>{greeting}</span>
        </div>
    )
};

export default ItemListContainer;